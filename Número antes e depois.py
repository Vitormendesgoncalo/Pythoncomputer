#This is one type to solve the problem
#Value = int(input('Enter an integer value: '))
#Previous = Value - 1
#Later = Value + 1
#print('The previous value is {}, the later value is {} and the original value is {}'.format(Previous, Later, Value))

# Second type to solve the problem
Value = int(input("Enter an integer value: "))
print ('The previous value is {}, the later value is {} and the original value is {}'.format(Value-1, Value+2, Value))